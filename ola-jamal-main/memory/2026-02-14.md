# Memory flush 2026-02-14

## OpenClaw
- Config: `~/.openclaw/openclaw.json`
- Modelo padrão: **anthropic/claude-opus-4-6** (Anthropic direto; chave em `env.ANTHROPIC_API_KEY`)
- Alternativa usada: **openrouter/z-ai/glm-5** (GLM 5 via OpenRouter)
- Gateway: `openclaw gateway restart`; dashboard http://127.0.0.1:18789/
- Telegram: @clawd_renato_bot (health: `openclaw health`)
- Respostas do Opus 4.6 podem levar **1–2 minutos**; esperar antes de concluir que “não responde”

## Projeto ola-jamal
- Backend .NET: rodar com `dotnet run` em `backend-dotnet/src/RenoveJa.Api`
- API: http://localhost:5000 — Swagger: http://localhost:5000/swagger
